<span>
  <button
    class='country-toggle'
    type='button'
    data-bs-toggle='collapse'
    data-bs-target='#navbarToggleExternalContent'
    aria-controls='navbarToggleExternalContent'
    aria-expanded='false'
    aria-label='Toggle navigation'
    {{on 'click' this.toggleNav}}
  >
    {{! <span class='navbar-toggler-icon'></span> }}
    <img
      src='assets/images/location.svg'
      alt='|'
      class='location-logo'
    />{{this.globalCountry}}
  </button>
</span>

<div class='sidebar' style={{(concat 'width: ' this.sidebarWidth 'px')}}>
  <div class='sidebar_top'>
    <label class='label' for='search_input'>Search</label>
    <input
      type='text'
      id='search_input'
      class='country__search'
      placeholder='Search ...'
      value={{this.searchText}}
      {{on 'input' this.filterCountry}}
    />
    <button
      type='button'
      class='close_btn'
      {{on 'click' this.toggleNav}}
    >&times;</button>
  </div>

  <div class='d-flex flex-column justify-content-start'>
    {{#each this.countryList as |country|}}
      {{#unless (eq country.countryName 'India')}}
        <button
          type='button'
          id={{this.countryId country.countryName}}
          class='country__name
            {{if
              (eq this.country.selectedCountry country.countryCode)
              "active"
            }}'
          {{on 'click' (fn this.selectCountry country.countryCode)}}
        >{{country.countryName}}</button>
      {{/unless}}
    {{/each}}
  </div>
</div>